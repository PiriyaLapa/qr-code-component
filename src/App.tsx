import { Component, ReactNode } from "react";

export default class App extends Component {
  render():ReactNode {
    return (
      <div>
        <h1>Hello QR-code component</h1>
      </div>
    );
  }
}
